import { calculatorRegistry } from '../../data/calculatorRegistry';
import { debt_consolidation_calculatorCalculatorCalculator } from './debt_consolidation_calculatorCalculatorCalculator';

export function registerdebt_consolidation_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new debt_consolidation_calculatorCalculatorCalculator());
}

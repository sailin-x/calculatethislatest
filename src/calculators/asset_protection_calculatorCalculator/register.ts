import { calculatorRegistry } from '../../data/calculatorRegistry';
import { asset_protection_calculatorCalculatorCalculator } from './asset_protection_calculatorCalculatorCalculator';

export function registerasset_protection_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new asset_protection_calculatorCalculatorCalculator());
}

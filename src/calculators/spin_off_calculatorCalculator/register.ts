import { calculatorRegistry } from '../../data/calculatorRegistry';
import { spin_off_calculatorCalculatorCalculator } from './spin_off_calculatorCalculatorCalculator';

export function registerspin_off_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new spin_off_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { physics_calculatorCalculatorCalculator } from './physics_calculatorCalculatorCalculator';

export function registerphysics_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new physics_calculatorCalculatorCalculator());
}

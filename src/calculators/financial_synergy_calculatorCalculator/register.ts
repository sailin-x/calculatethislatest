import { calculatorRegistry } from '../../data/calculatorRegistry';
import { financial_synergy_calculatorCalculatorCalculator } from './financial_synergy_calculatorCalculatorCalculator';

export function registerfinancial_synergy_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new financial_synergy_calculatorCalculatorCalculator());
}

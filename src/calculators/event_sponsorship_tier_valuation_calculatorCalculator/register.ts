import { calculatorRegistry } from '../../data/calculatorRegistry';
import { event_sponsorship_tier_valuation_calculatorCalculatorCalculator } from './event_sponsorship_tier_valuation_calculatorCalculatorCalculator';

export function registerevent_sponsorship_tier_valuation_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new event_sponsorship_tier_valuation_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { foundation_repair_cost_calculatorCalculatorCalculator } from './foundation_repair_cost_calculatorCalculatorCalculator';

export function registerfoundation_repair_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new foundation_repair_cost_calculatorCalculatorCalculator());
}

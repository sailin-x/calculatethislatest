import { calculatorRegistry } from '../../data/calculatorRegistry';
import { bankruptcy_filing_calculatorCalculatorCalculator } from './bankruptcy_filing_calculatorCalculatorCalculator';

export function registerbankruptcy_filing_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new bankruptcy_filing_calculatorCalculatorCalculator());
}

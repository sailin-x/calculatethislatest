import { calculatorRegistry } from '../../data/calculatorRegistry';
import { storm_damage_repair_cost_calculatorCalculatorCalculator } from './storm_damage_repair_cost_calculatorCalculatorCalculator';

export function registerstorm_damage_repair_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new storm_damage_repair_cost_calculatorCalculatorCalculator());
}

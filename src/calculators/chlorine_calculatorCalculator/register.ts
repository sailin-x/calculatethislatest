import { calculatorRegistry } from '../../data/calculatorRegistry';
import { chlorine_calculatorCalculatorCalculator } from './chlorine_calculatorCalculatorCalculator';

export function registerchlorine_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new chlorine_calculatorCalculatorCalculator());
}

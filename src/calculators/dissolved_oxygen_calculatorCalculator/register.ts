import { calculatorRegistry } from '../../data/calculatorRegistry';
import { dissolved_oxygen_calculatorCalculatorCalculator } from './dissolved_oxygen_calculatorCalculatorCalculator';

export function registerdissolved_oxygen_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new dissolved_oxygen_calculatorCalculatorCalculator());
}

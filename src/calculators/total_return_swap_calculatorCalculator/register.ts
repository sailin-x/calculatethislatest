import { calculatorRegistry } from '../../data/calculatorRegistry';
import { total_return_swap_calculatorCalculatorCalculator } from './total_return_swap_calculatorCalculatorCalculator';

export function registertotal_return_swap_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new total_return_swap_calculatorCalculatorCalculator());
}

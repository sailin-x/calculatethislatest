import { calculatorRegistry } from '../../data/calculatorRegistry';
import { './construction/concrete';Calculator } from './'./construction/concrete';Calculator';

export function register'./construction/concrete';Calculator(): void {
  calculatorRegistry.register(new './construction/concrete';Calculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { './construction/concrete/concrete';Calculator } from './'./construction/concrete/concrete';Calculator';

export function register'./construction/concrete/concrete';Calculator(): void {
  calculatorRegistry.register(new './construction/concrete/concrete';Calculator());
}

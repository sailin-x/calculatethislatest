import { calculatorRegistry } from '../../data/calculatorRegistry';
import { './health/bmi-calculator/bmi_calculator';Calculator } from './'./health/bmi-calculator/bmi_calculator';Calculator';

export function register'./health/bmi-calculator/bmi_calculator';Calculator(): void {
  calculatorRegistry.register(new './health/bmi-calculator/bmi_calculator';Calculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { './health/bmi-calculator/bmi-calculator';Calculator } from './'./health/bmi-calculator/bmi-calculator';Calculator';

export function register'./health/bmi-calculator/bmi-calculator';Calculator(): void {
  calculatorRegistry.register(new './health/bmi-calculator/bmi-calculator';Calculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { './math/fraction-calculator/fraction_calculator';Calculator } from './'./math/fraction-calculator/fraction_calculator';Calculator';

export function register'./math/fraction-calculator/fraction_calculator';Calculator(): void {
  calculatorRegistry.register(new './math/fraction-calculator/fraction_calculator';Calculator());
}

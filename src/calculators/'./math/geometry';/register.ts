import { calculatorRegistry } from '../../data/calculatorRegistry';
import { './math/geometry';Calculator } from './'./math/geometry';Calculator';

export function register'./math/geometry';Calculator(): void {
  calculatorRegistry.register(new './math/geometry';Calculator());
}

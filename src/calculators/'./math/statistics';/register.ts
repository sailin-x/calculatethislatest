import { calculatorRegistry } from '../../data/calculatorRegistry';
import { './math/statistics';Calculator } from './'./math/statistics';Calculator';

export function register'./math/statistics';Calculator(): void {
  calculatorRegistry.register(new './math/statistics';Calculator());
}

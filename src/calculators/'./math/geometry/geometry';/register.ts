import { calculatorRegistry } from '../../data/calculatorRegistry';
import { './math/geometry/geometry';Calculator } from './'./math/geometry/geometry';Calculator';

export function register'./math/geometry/geometry';Calculator(): void {
  calculatorRegistry.register(new './math/geometry/geometry';Calculator());
}

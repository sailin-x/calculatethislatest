import { calculatorRegistry } from '../../data/calculatorRegistry';
import { './math/matrix';Calculator } from './'./math/matrix';Calculator';

export function register'./math/matrix';Calculator(): void {
  calculatorRegistry.register(new './math/matrix';Calculator());
}

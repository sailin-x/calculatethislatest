import { calculatorRegistry } from '../../data/calculatorRegistry';
import { './finance/loan-to-cost/loan_to_cost';Calculator } from './'./finance/loan-to-cost/loan_to_cost';Calculator';

export function register'./finance/loan-to-cost/loan_to_cost';Calculator(): void {
  calculatorRegistry.register(new './finance/loan-to-cost/loan_to_cost';Calculator());
}

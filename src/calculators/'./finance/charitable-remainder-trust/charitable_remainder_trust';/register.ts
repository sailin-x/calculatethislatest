import { calculatorRegistry } from '../../data/calculatorRegistry';
import { './finance/charitable-remainder-trust/charitable_remainder_trust';Calculator } from './'./finance/charitable-remainder-trust/charitable_remainder_trust';Calculator';

export function register'./finance/charitable-remainder-trust/charitable_remainder_trust';Calculator(): void {
  calculatorRegistry.register(new './finance/charitable-remainder-trust/charitable_remainder_trust';Calculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { aquarium_cost_calculatorCalculatorCalculator } from './aquarium_cost_calculatorCalculatorCalculator';

export function registeraquarium_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new aquarium_cost_calculatorCalculatorCalculator());
}

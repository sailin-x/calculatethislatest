import { calculatorRegistry } from '../../data/calculatorRegistry';
import { reit_dividend_calculatorCalculatorCalculator } from './reit_dividend_calculatorCalculatorCalculator';

export function registerreit_dividend_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new reit_dividend_calculatorCalculatorCalculator());
}

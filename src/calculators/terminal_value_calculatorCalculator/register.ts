import { calculatorRegistry } from '../../data/calculatorRegistry';
import { terminal_value_calculatorCalculatorCalculator } from './terminal_value_calculatorCalculatorCalculator';

export function registerterminal_value_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new terminal_value_calculatorCalculatorCalculator());
}

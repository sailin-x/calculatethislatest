import { calculatorRegistry } from '../../data/calculatorRegistry';
import { networking_calculatorCalculatorCalculator } from './networking_calculatorCalculatorCalculator';

export function registernetworking_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new networking_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { cholesterol_calculatorCalculatorCalculator } from './cholesterol_calculatorCalculatorCalculator';

export function registercholesterol_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new cholesterol_calculatorCalculatorCalculator());
}

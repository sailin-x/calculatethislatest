import { calculatorRegistry } from '../../data/calculatorRegistry';
import { franchise_roi_calculatorCalculatorCalculator } from './franchise_roi_calculatorCalculatorCalculator';

export function registerfranchise_roi_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new franchise_roi_calculatorCalculatorCalculator());
}

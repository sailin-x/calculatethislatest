import { calculatorRegistry } from '../../data/calculatorRegistry';
import { insulation_calculatorCalculatorCalculator } from './insulation_calculatorCalculatorCalculator';

export function registerinsulation_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new insulation_calculatorCalculatorCalculator());
}

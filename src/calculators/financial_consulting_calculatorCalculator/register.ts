import { calculatorRegistry } from '../../data/calculatorRegistry';
import { financial_consulting_calculatorCalculatorCalculator } from './financial_consulting_calculatorCalculatorCalculator';

export function registerfinancial_consulting_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new financial_consulting_calculatorCalculatorCalculator());
}

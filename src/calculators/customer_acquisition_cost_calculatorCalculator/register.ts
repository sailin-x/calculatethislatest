import { calculatorRegistry } from '../../data/calculatorRegistry';
import { customer_acquisition_cost_calculatorCalculatorCalculator } from './customer_acquisition_cost_calculatorCalculatorCalculator';

export function registercustomer_acquisition_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new customer_acquisition_cost_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { podcast_cost_calculatorCalculatorCalculator } from './podcast_cost_calculatorCalculatorCalculator';

export function registerpodcast_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new podcast_cost_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { algebra_calculatorCalculatorCalculator } from './algebra_calculatorCalculatorCalculator';

export function registeralgebra_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new algebra_calculatorCalculatorCalculator());
}

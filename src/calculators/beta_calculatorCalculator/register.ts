import { calculatorRegistry } from '../../data/calculatorRegistry';
import { beta_calculatorCalculatorCalculator } from './beta_calculatorCalculatorCalculator';

export function registerbeta_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new beta_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { meal_kit_cost_calculatorCalculatorCalculator } from './meal_kit_cost_calculatorCalculatorCalculator';

export function registermeal_kit_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new meal_kit_cost_calculatorCalculatorCalculator());
}

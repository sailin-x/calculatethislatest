import { calculatorRegistry } from '../../data/calculatorRegistry';
import { invoice_financing_calculatorCalculatorCalculator } from './invoice_financing_calculatorCalculatorCalculator';

export function registerinvoice_financing_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new invoice_financing_calculatorCalculatorCalculator());
}

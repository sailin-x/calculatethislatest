import { calculatorRegistry } from '../../data/calculatorRegistry';
import { probability_calculatorCalculatorCalculator } from './probability_calculatorCalculatorCalculator';

export function registerprobability_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new probability_calculatorCalculatorCalculator());
}

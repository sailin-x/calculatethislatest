import { calculatorRegistry } from '../../data/calculatorRegistry';
import { fraction_calculatorCalculatorCalculator } from './fraction_calculatorCalculatorCalculator';

export function registerfraction_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new fraction_calculatorCalculatorCalculator());
}

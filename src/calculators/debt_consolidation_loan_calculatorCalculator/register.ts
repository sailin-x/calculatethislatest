import { calculatorRegistry } from '../../data/calculatorRegistry';
import { debt_consolidation_loan_calculatorCalculatorCalculator } from './debt_consolidation_loan_calculatorCalculatorCalculator';

export function registerdebt_consolidation_loan_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new debt_consolidation_loan_calculatorCalculatorCalculator());
}

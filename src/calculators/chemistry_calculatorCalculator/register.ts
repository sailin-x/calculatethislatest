import { calculatorRegistry } from '../../data/calculatorRegistry';
import { chemistry_calculatorCalculatorCalculator } from './chemistry_calculatorCalculatorCalculator';

export function registerchemistry_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new chemistry_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { jones_act_settlement_calculatorCalculatorCalculator } from './jones_act_settlement_calculatorCalculatorCalculator';

export function registerjones_act_settlement_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new jones_act_settlement_calculatorCalculatorCalculator());
}

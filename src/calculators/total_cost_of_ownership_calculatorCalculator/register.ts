import { calculatorRegistry } from '../../data/calculatorRegistry';
import { total_cost_of_ownership_calculatorCalculatorCalculator } from './total_cost_of_ownership_calculatorCalculatorCalculator';

export function registertotal_cost_of_ownership_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new total_cost_of_ownership_calculatorCalculatorCalculator());
}

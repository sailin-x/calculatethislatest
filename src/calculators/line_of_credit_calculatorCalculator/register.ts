import { calculatorRegistry } from '../../data/calculatorRegistry';
import { line_of_credit_calculatorCalculatorCalculator } from './line_of_credit_calculatorCalculatorCalculator';

export function registerline_of_credit_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new line_of_credit_calculatorCalculatorCalculator());
}

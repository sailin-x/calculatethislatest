import { calculatorRegistry } from '../../data/calculatorRegistry';
import { flooring_calculatorCalculatorCalculator } from './flooring_calculatorCalculatorCalculator';

export function registerflooring_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new flooring_calculatorCalculatorCalculator());
}

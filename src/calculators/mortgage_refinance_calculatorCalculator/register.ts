import { calculatorRegistry } from '../../data/calculatorRegistry';
import { mortgage_refinance_calculatorCalculatorCalculator } from './mortgage_refinance_calculatorCalculatorCalculator';

export function registermortgage_refinance_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new mortgage_refinance_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { marine_insurance_calculatorCalculatorCalculator } from './marine_insurance_calculatorCalculatorCalculator';

export function registermarine_insurance_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new marine_insurance_calculatorCalculatorCalculator());
}

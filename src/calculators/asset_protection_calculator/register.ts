import { calculatorRegistry } from '../../data/calculatorRegistry';
import {  } from './';

export function registerasset_protection_calculator(): void {
  calculatorRegistry.register(asset_protection_calculator);
}

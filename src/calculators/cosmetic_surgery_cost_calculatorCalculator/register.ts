import { calculatorRegistry } from '../../data/calculatorRegistry';
import { cosmetic_surgery_cost_calculatorCalculatorCalculator } from './cosmetic_surgery_cost_calculatorCalculatorCalculator';

export function registercosmetic_surgery_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new cosmetic_surgery_cost_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { directors_officers_insurance_calculatorCalculatorCalculator } from './directors_officers_insurance_calculatorCalculatorCalculator';

export function registerdirectors_officers_insurance_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new directors_officers_insurance_calculatorCalculatorCalculator());
}

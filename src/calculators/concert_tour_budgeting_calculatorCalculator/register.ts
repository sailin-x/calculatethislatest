import { calculatorRegistry } from '../../data/calculatorRegistry';
import { concert_tour_budgeting_calculatorCalculatorCalculator } from './concert_tour_budgeting_calculatorCalculatorCalculator';

export function registerconcert_tour_budgeting_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new concert_tour_budgeting_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { premium_deficiency_reserve_calculatorCalculatorCalculator } from './premium_deficiency_reserve_calculatorCalculatorCalculator';

export function registerpremium_deficiency_reserve_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new premium_deficiency_reserve_calculatorCalculatorCalculator());
}

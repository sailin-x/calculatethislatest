import { calculatorRegistry } from '../../data/calculatorRegistry';
import { passport_cost_calculatorCalculatorCalculator } from './passport_cost_calculatorCalculatorCalculator';

export function registerpassport_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new passport_cost_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { contract_review_calculatorCalculatorCalculator } from './contract_review_calculatorCalculatorCalculator';

export function registercontract_review_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new contract_review_calculatorCalculatorCalculator());
}

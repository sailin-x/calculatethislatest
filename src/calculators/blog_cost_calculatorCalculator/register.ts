import { calculatorRegistry } from '../../data/calculatorRegistry';
import { blog_cost_calculatorCalculatorCalculator } from './blog_cost_calculatorCalculatorCalculator';

export function registerblog_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new blog_cost_calculatorCalculatorCalculator());
}

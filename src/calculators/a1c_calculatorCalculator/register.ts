import { calculatorRegistry } from '../../data/calculatorRegistry';
import { a1c_calculatorCalculatorCalculator } from './a1c_calculatorCalculatorCalculator';

export function registera1c_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new a1c_calculatorCalculatorCalculator());
}

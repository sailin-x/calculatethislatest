import { calculatorRegistry } from '../../data/calculatorRegistry';
import { currency_exchange_cost_calculatorCalculatorCalculator } from './currency_exchange_cost_calculatorCalculatorCalculator';

export function registercurrency_exchange_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new currency_exchange_cost_calculatorCalculatorCalculator());
}

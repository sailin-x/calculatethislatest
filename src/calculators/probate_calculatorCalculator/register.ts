import { calculatorRegistry } from '../../data/calculatorRegistry';
import { probate_calculatorCalculatorCalculator } from './probate_calculatorCalculatorCalculator';

export function registerprobate_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new probate_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { garden_calculatorCalculatorCalculator } from './garden_calculatorCalculatorCalculator';

export function registergarden_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new garden_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { startup_valuation_calculatorCalculatorCalculator } from './startup_valuation_calculatorCalculatorCalculator';

export function registerstartup_valuation_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new startup_valuation_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { body_fat_calculatorCalculatorCalculator } from './body_fat_calculatorCalculatorCalculator';

export function registerbody_fat_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new body_fat_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { biology_calculatorCalculatorCalculator } from './biology_calculatorCalculatorCalculator';

export function registerbiology_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new biology_calculatorCalculatorCalculator());
}

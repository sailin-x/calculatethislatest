import { calculatorRegistry } from '../../data/calculatorRegistry';
import { estrogen_calculatorCalculatorCalculator } from './estrogen_calculatorCalculatorCalculator';

export function registerestrogen_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new estrogen_calculatorCalculatorCalculator());
}

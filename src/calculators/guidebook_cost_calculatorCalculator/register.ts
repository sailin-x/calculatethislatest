import { calculatorRegistry } from '../../data/calculatorRegistry';
import { guidebook_cost_calculatorCalculatorCalculator } from './guidebook_cost_calculatorCalculatorCalculator';

export function registerguidebook_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new guidebook_cost_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { paint_calculatorCalculatorCalculator } from './paint_calculatorCalculatorCalculator';

export function registerpaint_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new paint_calculatorCalculatorCalculator());
}

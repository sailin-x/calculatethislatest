import { calculatorRegistry } from '../../data/calculatorRegistry';
import { analytics_cost_calculatorCalculatorCalculator } from './analytics_cost_calculatorCalculatorCalculator';

export function registeranalytics_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new analytics_cost_calculatorCalculatorCalculator());
}

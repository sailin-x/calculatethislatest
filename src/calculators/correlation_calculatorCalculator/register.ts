import { calculatorRegistry } from '../../data/calculatorRegistry';
import { correlation_calculatorCalculatorCalculator } from './correlation_calculatorCalculatorCalculator';

export function registercorrelation_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new correlation_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { movie_ticket_cost_calculatorCalculatorCalculator } from './movie_ticket_cost_calculatorCalculatorCalculator';

export function registermovie_ticket_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new movie_ticket_cost_calculatorCalculatorCalculator());
}

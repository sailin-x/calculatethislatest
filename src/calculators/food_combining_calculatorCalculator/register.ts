import { calculatorRegistry } from '../../data/calculatorRegistry';
import { food_combining_calculatorCalculatorCalculator } from './food_combining_calculatorCalculatorCalculator';

export function registerfood_combining_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new food_combining_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { sales_commission_structure_calculatorCalculatorCalculator } from './sales_commission_structure_calculatorCalculatorCalculator';

export function registersales_commission_structure_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new sales_commission_structure_calculatorCalculatorCalculator());
}

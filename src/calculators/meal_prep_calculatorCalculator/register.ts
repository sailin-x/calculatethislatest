import { calculatorRegistry } from '../../data/calculatorRegistry';
import { meal_prep_calculatorCalculatorCalculator } from './meal_prep_calculatorCalculatorCalculator';

export function registermeal_prep_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new meal_prep_calculatorCalculatorCalculator());
}

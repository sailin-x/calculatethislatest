import { CalculatorRegistry } from '../../../data/calculatorRegistry';
import { RefinanceCalculator } from './RefinanceCalculator';

// Register the Refinance Calculator
CalculatorRegistry.register(RefinanceCalculator);

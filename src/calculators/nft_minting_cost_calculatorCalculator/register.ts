import { calculatorRegistry } from '../../data/calculatorRegistry';
import { nft_minting_cost_calculatorCalculatorCalculator } from './nft_minting_cost_calculatorCalculatorCalculator';

export function registernft_minting_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new nft_minting_cost_calculatorCalculatorCalculator());
}

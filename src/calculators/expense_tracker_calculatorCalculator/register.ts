import { calculatorRegistry } from '../../data/calculatorRegistry';
import { expense_tracker_calculatorCalculatorCalculator } from './expense_tracker_calculatorCalculatorCalculator';

export function registerexpense_tracker_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new expense_tracker_calculatorCalculatorCalculator());
}

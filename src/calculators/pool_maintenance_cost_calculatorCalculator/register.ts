import { calculatorRegistry } from '../../data/calculatorRegistry';
import { pool_maintenance_cost_calculatorCalculatorCalculator } from './pool_maintenance_cost_calculatorCalculatorCalculator';

export function registerpool_maintenance_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new pool_maintenance_cost_calculatorCalculatorCalculator());
}

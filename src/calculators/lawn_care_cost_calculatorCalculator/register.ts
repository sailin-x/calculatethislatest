import { calculatorRegistry } from '../../data/calculatorRegistry';
import { lawn_care_cost_calculatorCalculatorCalculator } from './lawn_care_cost_calculatorCalculatorCalculator';

export function registerlawn_care_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new lawn_care_cost_calculatorCalculatorCalculator());
}

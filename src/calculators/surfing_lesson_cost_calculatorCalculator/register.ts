import { calculatorRegistry } from '../../data/calculatorRegistry';
import { surfing_lesson_cost_calculatorCalculatorCalculator } from './surfing_lesson_cost_calculatorCalculatorCalculator';

export function registersurfing_lesson_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new surfing_lesson_cost_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { immigration_filing_calculatorCalculatorCalculator } from './immigration_filing_calculatorCalculatorCalculator';

export function registerimmigration_filing_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new immigration_filing_calculatorCalculatorCalculator());
}

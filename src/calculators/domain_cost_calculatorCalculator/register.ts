import { calculatorRegistry } from '../../data/calculatorRegistry';
import { domain_cost_calculatorCalculatorCalculator } from './domain_cost_calculatorCalculatorCalculator';

export function registerdomain_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new domain_cost_calculatorCalculatorCalculator());
}

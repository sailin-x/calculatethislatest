import { calculatorRegistry } from '../../data/calculatorRegistry';
import { soil_amendment_calculatorCalculatorCalculator } from './soil_amendment_calculatorCalculatorCalculator';

export function registersoil_amendment_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new soil_amendment_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { database_calculatorCalculatorCalculator } from './database_calculatorCalculatorCalculator';

export function registerdatabase_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new database_calculatorCalculatorCalculator());
}

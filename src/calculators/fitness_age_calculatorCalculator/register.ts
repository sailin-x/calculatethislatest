import { calculatorRegistry } from '../../data/calculatorRegistry';
import { fitness_age_calculatorCalculatorCalculator } from './fitness_age_calculatorCalculatorCalculator';

export function registerfitness_age_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new fitness_age_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { capm_calculatorCalculatorCalculator } from './capm_calculatorCalculatorCalculator';

export function registercapm_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new capm_calculatorCalculatorCalculator());
}

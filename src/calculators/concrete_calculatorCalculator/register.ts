import { calculatorRegistry } from '../../data/calculatorRegistry';
import { concrete_calculatorCalculatorCalculator } from './concrete_calculatorCalculatorCalculator';

export function registerconcrete_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new concrete_calculatorCalculatorCalculator());
}

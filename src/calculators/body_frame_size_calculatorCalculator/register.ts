import { calculatorRegistry } from '../../data/calculatorRegistry';
import { body_frame_size_calculatorCalculatorCalculator } from './body_frame_size_calculatorCalculatorCalculator';

export function registerbody_frame_size_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new body_frame_size_calculatorCalculatorCalculator());
}

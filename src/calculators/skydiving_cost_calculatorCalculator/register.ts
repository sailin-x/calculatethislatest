import { calculatorRegistry } from '../../data/calculatorRegistry';
import { skydiving_cost_calculatorCalculatorCalculator } from './skydiving_cost_calculatorCalculatorCalculator';

export function registerskydiving_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new skydiving_cost_calculatorCalculatorCalculator());
}

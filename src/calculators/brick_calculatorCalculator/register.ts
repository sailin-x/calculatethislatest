import { calculatorRegistry } from '../../data/calculatorRegistry';
import { brick_calculatorCalculatorCalculator } from './brick_calculatorCalculatorCalculator';

export function registerbrick_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new brick_calculatorCalculatorCalculator());
}

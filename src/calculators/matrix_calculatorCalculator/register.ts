import { calculatorRegistry } from '../../data/calculatorRegistry';
import { matrix_calculatorCalculatorCalculator } from './matrix_calculatorCalculatorCalculator';

export function registermatrix_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new matrix_calculatorCalculatorCalculator());
}

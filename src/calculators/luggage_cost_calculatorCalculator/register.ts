import { calculatorRegistry } from '../../data/calculatorRegistry';
import { luggage_cost_calculatorCalculatorCalculator } from './luggage_cost_calculatorCalculatorCalculator';

export function registerluggage_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new luggage_cost_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { dividend_discount_model_ddm_calculatorCalculatorCalculator } from './dividend_discount_model_ddm_calculatorCalculatorCalculator';

export function registerdividend_discount_model_ddm_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new dividend_discount_model_ddm_calculatorCalculatorCalculator());
}

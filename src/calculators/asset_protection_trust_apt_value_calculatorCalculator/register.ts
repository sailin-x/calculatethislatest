import { calculatorRegistry } from '../../data/calculatorRegistry';
import { asset_protection_trust_apt_value_calculatorCalculatorCalculator } from './asset_protection_trust_apt_value_calculatorCalculatorCalculator';

export function registerasset_protection_trust_apt_value_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new asset_protection_trust_apt_value_calculatorCalculatorCalculator());
}

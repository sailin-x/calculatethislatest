import { calculatorRegistry } from '../../data/calculatorRegistry';
import { taxi_cost_calculatorCalculatorCalculator } from './taxi_cost_calculatorCalculatorCalculator';

export function registertaxi_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new taxi_cost_calculatorCalculatorCalculator());
}

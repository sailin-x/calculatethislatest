import { calculatorRegistry } from '../../data/calculatorRegistry';
import { snowboard_lesson_cost_calculatorCalculatorCalculator } from './snowboard_lesson_cost_calculatorCalculatorCalculator';

export function registersnowboard_lesson_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new snowboard_lesson_cost_calculatorCalculatorCalculator());
}

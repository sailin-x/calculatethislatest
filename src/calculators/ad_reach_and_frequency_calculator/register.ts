import { calculatorRegistry } from '../../data/calculatorRegistry';
import {  } from './';

export function registerad_reach_and_frequency_calculator(): void {
  calculatorRegistry.register(ad_reach_and_frequency_calculator);
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { loan_comparison_calculatorCalculatorCalculator } from './loan_comparison_calculatorCalculatorCalculator';

export function registerloan_comparison_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new loan_comparison_calculatorCalculatorCalculator());
}

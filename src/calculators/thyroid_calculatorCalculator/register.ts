import { calculatorRegistry } from '../../data/calculatorRegistry';
import { thyroid_calculatorCalculatorCalculator } from './thyroid_calculatorCalculatorCalculator';

export function registerthyroid_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new thyroid_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { percentage_calculatorCalculatorCalculator } from './percentage_calculatorCalculatorCalculator';

export function registerpercentage_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new percentage_calculatorCalculatorCalculator());
}

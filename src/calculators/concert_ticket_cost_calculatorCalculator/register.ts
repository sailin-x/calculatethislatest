import { calculatorRegistry } from '../../data/calculatorRegistry';
import { concert_ticket_cost_calculatorCalculatorCalculator } from './concert_ticket_cost_calculatorCalculatorCalculator';

export function registerconcert_ticket_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new concert_ticket_cost_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { combined_ratio_calculatorCalculatorCalculator } from './combined_ratio_calculatorCalculatorCalculator';

export function registercombined_ratio_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new combined_ratio_calculatorCalculatorCalculator());
}

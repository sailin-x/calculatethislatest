import { calculatorRegistry } from '../../data/calculatorRegistry';
import { sortino_ratio_calculatorCalculatorCalculator } from './sortino_ratio_calculatorCalculatorCalculator';

export function registersortino_ratio_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new sortino_ratio_calculatorCalculatorCalculator());
}

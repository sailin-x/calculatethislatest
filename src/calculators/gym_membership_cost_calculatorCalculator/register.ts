import { calculatorRegistry } from '../../data/calculatorRegistry';
import { gym_membership_cost_calculatorCalculatorCalculator } from './gym_membership_cost_calculatorCalculatorCalculator';

export function registergym_membership_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new gym_membership_cost_calculatorCalculatorCalculator());
}

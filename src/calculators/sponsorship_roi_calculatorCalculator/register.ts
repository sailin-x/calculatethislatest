import { calculatorRegistry } from '../../data/calculatorRegistry';
import { sponsorship_roi_calculatorCalculatorCalculator } from './sponsorship_roi_calculatorCalculatorCalculator';

export function registersponsorship_roi_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new sponsorship_roi_calculatorCalculatorCalculator());
}

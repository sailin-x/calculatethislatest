import { calculatorRegistry } from '../../data/calculatorRegistry';
import { record_label_deal_calculatorCalculatorCalculator } from './record_label_deal_calculatorCalculatorCalculator';

export function registerrecord_label_deal_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new record_label_deal_calculatorCalculatorCalculator());
}

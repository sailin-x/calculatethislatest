import { calculatorRegistry } from '../../data/calculatorRegistry';
import { financial_strategy_calculatorCalculatorCalculator } from './financial_strategy_calculatorCalculatorCalculator';

export function registerfinancial_strategy_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new financial_strategy_calculatorCalculatorCalculator());
}

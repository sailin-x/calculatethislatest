import { calculatorRegistry } from '../../data/calculatorRegistry';
import { fixed_index_annuity_calculatorCalculatorCalculator } from './fixed_index_annuity_calculatorCalculatorCalculator';

export function registerfixed_index_annuity_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new fixed_index_annuity_calculatorCalculatorCalculator());
}

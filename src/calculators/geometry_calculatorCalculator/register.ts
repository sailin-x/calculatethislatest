import { calculatorRegistry } from '../../data/calculatorRegistry';
import { geometry_calculatorCalculatorCalculator } from './geometry_calculatorCalculatorCalculator';

export function registergeometry_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new geometry_calculatorCalculatorCalculator());
}

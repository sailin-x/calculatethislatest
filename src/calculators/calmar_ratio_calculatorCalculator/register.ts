import { calculatorRegistry } from '../../data/calculatorRegistry';
import { calmar_ratio_calculatorCalculatorCalculator } from './calmar_ratio_calculatorCalculatorCalculator';

export function registercalmar_ratio_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new calmar_ratio_calculatorCalculatorCalculator());
}

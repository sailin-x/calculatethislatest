import { calculatorRegistry } from '../../data/calculatorRegistry';
import { spin_class_cost_calculatorCalculatorCalculator } from './spin_class_cost_calculatorCalculatorCalculator';

export function registerspin_class_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new spin_class_cost_calculatorCalculatorCalculator());
}

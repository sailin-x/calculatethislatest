import { calculatorRegistry } from '../../data/calculatorRegistry';
import { email_marketing_cost_calculatorCalculatorCalculator } from './email_marketing_cost_calculatorCalculatorCalculator';

export function registeremail_marketing_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new email_marketing_cost_calculatorCalculatorCalculator());
}

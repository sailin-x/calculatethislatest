import { calculatorRegistry } from '../../data/calculatorRegistry';
import { ira_calculatorCalculatorCalculator } from './ira_calculatorCalculatorCalculator';

export function registerira_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new ira_calculatorCalculatorCalculator());
}

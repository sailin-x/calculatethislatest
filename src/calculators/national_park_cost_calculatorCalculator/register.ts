import { calculatorRegistry } from '../../data/calculatorRegistry';
import { national_park_cost_calculatorCalculatorCalculator } from './national_park_cost_calculatorCalculatorCalculator';

export function registernational_park_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new national_park_cost_calculatorCalculatorCalculator());
}

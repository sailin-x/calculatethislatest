import { calculatorRegistry } from '../../data/calculatorRegistry';
import { barre_class_cost_calculatorCalculatorCalculator } from './barre_class_cost_calculatorCalculatorCalculator';

export function registerbarre_class_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new barre_class_cost_calculatorCalculatorCalculator());
}

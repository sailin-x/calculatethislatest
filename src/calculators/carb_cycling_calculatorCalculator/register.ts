import { calculatorRegistry } from '../../data/calculatorRegistry';
import { carb_cycling_calculatorCalculatorCalculator } from './carb_cycling_calculatorCalculatorCalculator';

export function registercarb_cycling_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new carb_cycling_calculatorCalculatorCalculator());
}

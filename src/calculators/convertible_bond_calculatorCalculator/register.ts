import { calculatorRegistry } from '../../data/calculatorRegistry';
import { convertible_bond_calculatorCalculatorCalculator } from './convertible_bond_calculatorCalculatorCalculator';

export function registerconvertible_bond_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new convertible_bond_calculatorCalculatorCalculator());
}

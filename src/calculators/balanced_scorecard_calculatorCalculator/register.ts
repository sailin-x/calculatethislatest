import { calculatorRegistry } from '../../data/calculatorRegistry';
import { balanced_scorecard_calculatorCalculatorCalculator } from './balanced_scorecard_calculatorCalculatorCalculator';

export function registerbalanced_scorecard_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new balanced_scorecard_calculatorCalculatorCalculator());
}

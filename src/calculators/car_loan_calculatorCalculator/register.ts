import { calculatorRegistry } from '../../data/calculatorRegistry';
import { car_loan_calculatorCalculatorCalculator } from './car_loan_calculatorCalculatorCalculator';

export function registercar_loan_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new car_loan_calculatorCalculatorCalculator());
}

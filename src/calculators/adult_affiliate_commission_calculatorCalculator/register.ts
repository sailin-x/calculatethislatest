import { calculatorRegistry } from '../../data/calculatorRegistry';
import { adult_affiliate_commission_calculatorCalculatorCalculator } from './adult_affiliate_commission_calculatorCalculatorCalculator';

export function registeradult_affiliate_commission_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new adult_affiliate_commission_calculatorCalculatorCalculator());
}

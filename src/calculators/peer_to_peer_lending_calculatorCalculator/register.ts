import { calculatorRegistry } from '../../data/calculatorRegistry';
import { peer_to_peer_lending_calculatorCalculatorCalculator } from './peer_to_peer_lending_calculatorCalculatorCalculator';

export function registerpeer_to_peer_lending_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new peer_to_peer_lending_calculatorCalculatorCalculator());
}

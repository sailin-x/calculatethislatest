import { calculatorRegistry } from '../../data/calculatorRegistry';
import { roi_calculatorCalculatorCalculator } from './roi_calculatorCalculatorCalculator';

export function registerroi_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new roi_calculatorCalculatorCalculator());
}

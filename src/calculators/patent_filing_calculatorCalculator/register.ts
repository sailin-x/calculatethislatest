import { calculatorRegistry } from '../../data/calculatorRegistry';
import { patent_filing_calculatorCalculatorCalculator } from './patent_filing_calculatorCalculatorCalculator';

export function registerpatent_filing_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new patent_filing_calculatorCalculatorCalculator());
}

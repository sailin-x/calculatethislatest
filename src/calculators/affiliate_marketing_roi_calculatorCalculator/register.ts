import { calculatorRegistry } from '../../data/calculatorRegistry';
import { affiliate_marketing_roi_calculatorCalculatorCalculator } from './affiliate_marketing_roi_calculatorCalculatorCalculator';

export function registeraffiliate_marketing_roi_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new affiliate_marketing_roi_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { pest_control_cost_calculatorCalculatorCalculator } from './pest_control_cost_calculatorCalculatorCalculator';

export function registerpest_control_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new pest_control_cost_calculatorCalculatorCalculator());
}

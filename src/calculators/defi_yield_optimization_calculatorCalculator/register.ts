import { calculatorRegistry } from '../../data/calculatorRegistry';
import { defi_yield_optimization_calculatorCalculatorCalculator } from './defi_yield_optimization_calculatorCalculatorCalculator';

export function registerdefi_yield_optimization_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new defi_yield_optimization_calculatorCalculatorCalculator());
}

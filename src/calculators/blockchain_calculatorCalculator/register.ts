import { calculatorRegistry } from '../../data/calculatorRegistry';
import { blockchain_calculatorCalculatorCalculator } from './blockchain_calculatorCalculatorCalculator';

export function registerblockchain_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new blockchain_calculatorCalculatorCalculator());
}

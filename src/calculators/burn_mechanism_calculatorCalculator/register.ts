import { calculatorRegistry } from '../../data/calculatorRegistry';
import { burn_mechanism_calculatorCalculatorCalculator } from './burn_mechanism_calculatorCalculatorCalculator';

export function registerburn_mechanism_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new burn_mechanism_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { tile_calculatorCalculatorCalculator } from './tile_calculatorCalculatorCalculator';

export function registertile_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new tile_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { mortgage_calculatorCalculatorCalculator } from './mortgage_calculatorCalculatorCalculator';

export function registermortgage_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new mortgage_calculatorCalculatorCalculator());
}

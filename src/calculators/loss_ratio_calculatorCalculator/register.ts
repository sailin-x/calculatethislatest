import { calculatorRegistry } from '../../data/calculatorRegistry';
import { loss_ratio_calculatorCalculatorCalculator } from './loss_ratio_calculatorCalculatorCalculator';

export function registerloss_ratio_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new loss_ratio_calculatorCalculatorCalculator());
}

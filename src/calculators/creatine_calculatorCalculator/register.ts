import { calculatorRegistry } from '../../data/calculatorRegistry';
import { creatine_calculatorCalculatorCalculator } from './creatine_calculatorCalculatorCalculator';

export function registercreatine_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new creatine_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { franchise_calculatorCalculatorCalculator } from './franchise_calculatorCalculatorCalculator';

export function registerfranchise_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new franchise_calculatorCalculatorCalculator());
}

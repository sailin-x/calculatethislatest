import { calculatorRegistry } from '../../data/calculatorRegistry';
import { financial_resilience_calculatorCalculatorCalculator } from './financial_resilience_calculatorCalculatorCalculator';

export function registerfinancial_resilience_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new financial_resilience_calculatorCalculatorCalculator());
}

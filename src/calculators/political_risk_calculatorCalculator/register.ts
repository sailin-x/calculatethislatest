import { calculatorRegistry } from '../../data/calculatorRegistry';
import { political_risk_calculatorCalculatorCalculator } from './political_risk_calculatorCalculatorCalculator';

export function registerpolitical_risk_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new political_risk_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { painting_cost_calculatorCalculatorCalculator } from './painting_cost_calculatorCalculatorCalculator';

export function registerpainting_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new painting_cost_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { golf_lesson_cost_calculatorCalculatorCalculator } from './golf_lesson_cost_calculatorCalculatorCalculator';

export function registergolf_lesson_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new golf_lesson_cost_calculatorCalculatorCalculator());
}

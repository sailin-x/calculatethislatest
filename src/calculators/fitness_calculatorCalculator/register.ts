import { calculatorRegistry } from '../../data/calculatorRegistry';
import { fitness_calculatorCalculatorCalculator } from './fitness_calculatorCalculatorCalculator';

export function registerfitness_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new fitness_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { medical_bill_negotiation_calculatorCalculatorCalculator } from './medical_bill_negotiation_calculatorCalculatorCalculator';

export function registermedical_bill_negotiation_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new medical_bill_negotiation_calculatorCalculatorCalculator());
}

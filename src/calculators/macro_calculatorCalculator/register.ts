import { calculatorRegistry } from '../../data/calculatorRegistry';
import { macro_calculatorCalculatorCalculator } from './macro_calculatorCalculatorCalculator';

export function registermacro_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new macro_calculatorCalculatorCalculator());
}

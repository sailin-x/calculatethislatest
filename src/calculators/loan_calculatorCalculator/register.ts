import { calculatorRegistry } from '../../data/calculatorRegistry';
import { loan_calculatorCalculatorCalculator } from './loan_calculatorCalculatorCalculator';

export function registerloan_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new loan_calculatorCalculatorCalculator());
}

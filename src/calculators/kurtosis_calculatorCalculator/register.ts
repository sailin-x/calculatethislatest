import { calculatorRegistry } from '../../data/calculatorRegistry';
import { kurtosis_calculatorCalculatorCalculator } from './kurtosis_calculatorCalculatorCalculator';

export function registerkurtosis_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new kurtosis_calculatorCalculatorCalculator());
}

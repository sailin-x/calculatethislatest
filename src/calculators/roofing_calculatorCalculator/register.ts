import { calculatorRegistry } from '../../data/calculatorRegistry';
import { roofing_calculatorCalculatorCalculator } from './roofing_calculatorCalculatorCalculator';

export function registerroofing_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new roofing_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { merger_acquisition_divestiture_calculatorCalculatorCalculator } from './merger_acquisition_divestiture_calculatorCalculatorCalculator';

export function registermerger_acquisition_divestiture_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new merger_acquisition_divestiture_calculatorCalculatorCalculator());
}

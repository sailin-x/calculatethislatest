import { calculatorRegistry } from '../../data/calculatorRegistry';
import { dividend_calculatorCalculatorCalculator } from './dividend_calculatorCalculatorCalculator';

export function registerdividend_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new dividend_calculatorCalculatorCalculator());
}

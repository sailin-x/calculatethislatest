import { calculatorRegistry } from '../../data/calculatorRegistry';
import { payday_loan_calculatorCalculatorCalculator } from './payday_loan_calculatorCalculatorCalculator';

export function registerpayday_loan_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new payday_loan_calculatorCalculatorCalculator());
}

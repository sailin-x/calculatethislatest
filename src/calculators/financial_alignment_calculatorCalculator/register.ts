import { calculatorRegistry } from '../../data/calculatorRegistry';
import { financial_alignment_calculatorCalculatorCalculator } from './financial_alignment_calculatorCalculatorCalculator';

export function registerfinancial_alignment_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new financial_alignment_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { litigation_finance_roi_calculatorCalculatorCalculator } from './litigation_finance_roi_calculatorCalculatorCalculator';

export function registerlitigation_finance_roi_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new litigation_finance_roi_calculatorCalculatorCalculator());
}

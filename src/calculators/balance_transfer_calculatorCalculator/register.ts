import { calculatorRegistry } from '../../data/calculatorRegistry';
import { balance_transfer_calculatorCalculatorCalculator } from './balance_transfer_calculatorCalculatorCalculator';

export function registerbalance_transfer_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new balance_transfer_calculatorCalculatorCalculator());
}

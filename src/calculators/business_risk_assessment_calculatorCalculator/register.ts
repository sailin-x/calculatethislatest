import { calculatorRegistry } from '../../data/calculatorRegistry';
import { business_risk_assessment_calculatorCalculatorCalculator } from './business_risk_assessment_calculatorCalculatorCalculator';

export function registerbusiness_risk_assessment_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new business_risk_assessment_calculatorCalculatorCalculator());
}

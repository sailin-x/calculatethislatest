import { calculatorRegistry } from '../../data/calculatorRegistry';
import { baking_calculatorCalculatorCalculator } from './baking_calculatorCalculatorCalculator';

export function registerbaking_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new baking_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { tax_calculatorCalculatorCalculator } from './tax_calculatorCalculatorCalculator';

export function registertax_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new tax_calculatorCalculatorCalculator());
}

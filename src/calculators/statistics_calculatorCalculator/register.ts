import { calculatorRegistry } from '../../data/calculatorRegistry';
import { statistics_calculatorCalculatorCalculator } from './statistics_calculatorCalculatorCalculator';

export function registerstatistics_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new statistics_calculatorCalculatorCalculator());
}

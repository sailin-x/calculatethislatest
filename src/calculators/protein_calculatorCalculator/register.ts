import { calculatorRegistry } from '../../data/calculatorRegistry';
import { protein_calculatorCalculatorCalculator } from './protein_calculatorCalculatorCalculator';

export function registerprotein_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new protein_calculatorCalculatorCalculator());
}

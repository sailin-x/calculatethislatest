import { calculatorRegistry } from '../../data/calculatorRegistry';
import { directors_officers_calculatorCalculatorCalculator } from './directors_officers_calculatorCalculatorCalculator';

export function registerdirectors_officers_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new directors_officers_calculatorCalculatorCalculator());
}

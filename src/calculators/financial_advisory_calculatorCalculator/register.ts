import { calculatorRegistry } from '../../data/calculatorRegistry';
import { financial_advisory_calculatorCalculatorCalculator } from './financial_advisory_calculatorCalculatorCalculator';

export function registerfinancial_advisory_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new financial_advisory_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { theater_ticket_cost_calculatorCalculatorCalculator } from './theater_ticket_cost_calculatorCalculatorCalculator';

export function registertheater_ticket_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new theater_ticket_cost_calculatorCalculatorCalculator());
}

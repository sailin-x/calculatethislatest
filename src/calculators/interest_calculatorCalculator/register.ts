import { calculatorRegistry } from '../../data/calculatorRegistry';
import { interest_calculatorCalculatorCalculator } from './interest_calculatorCalculatorCalculator';

export function registerinterest_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new interest_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { financial_integration_calculatorCalculatorCalculator } from './financial_integration_calculatorCalculatorCalculator';

export function registerfinancial_integration_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new financial_integration_calculatorCalculatorCalculator());
}

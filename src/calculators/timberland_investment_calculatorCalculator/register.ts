import { calculatorRegistry } from '../../data/calculatorRegistry';
import { timberland_investment_calculatorCalculatorCalculator } from './timberland_investment_calculatorCalculatorCalculator';

export function registertimberland_investment_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new timberland_investment_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { patreon_subscription_calculatorCalculatorCalculator } from './patreon_subscription_calculatorCalculatorCalculator';

export function registerpatreon_subscription_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new patreon_subscription_calculatorCalculatorCalculator());
}

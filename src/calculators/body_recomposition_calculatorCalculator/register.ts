import { calculatorRegistry } from '../../data/calculatorRegistry';
import { body_recomposition_calculatorCalculatorCalculator } from './body_recomposition_calculatorCalculatorCalculator';

export function registerbody_recomposition_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new body_recomposition_calculatorCalculatorCalculator());
}

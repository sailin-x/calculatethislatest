import { calculatorRegistry } from '../../data/calculatorRegistry';
import { airdrop_value_calculatorCalculatorCalculator } from './airdrop_value_calculatorCalculatorCalculator';

export function registerairdrop_value_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new airdrop_value_calculatorCalculatorCalculator());
}

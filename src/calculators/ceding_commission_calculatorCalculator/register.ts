import { calculatorRegistry } from '../../data/calculatorRegistry';
import { ceding_commission_calculatorCalculatorCalculator } from './ceding_commission_calculatorCalculatorCalculator';

export function registerceding_commission_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new ceding_commission_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { antitrust_calculatorCalculatorCalculator } from './antitrust_calculatorCalculatorCalculator';

export function registerantitrust_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new antitrust_calculatorCalculatorCalculator());
}

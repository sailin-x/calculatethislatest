import { calculatorRegistry } from '../../data/calculatorRegistry';
import { podcast_sponsorship_calculatorCalculatorCalculator } from './podcast_sponsorship_calculatorCalculatorCalculator';

export function registerpodcast_sponsorship_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new podcast_sponsorship_calculatorCalculatorCalculator());
}

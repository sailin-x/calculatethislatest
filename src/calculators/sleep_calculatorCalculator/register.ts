import { calculatorRegistry } from '../../data/calculatorRegistry';
import { sleep_calculatorCalculatorCalculator } from './sleep_calculatorCalculatorCalculator';

export function registersleep_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new sleep_calculatorCalculatorCalculator());
}

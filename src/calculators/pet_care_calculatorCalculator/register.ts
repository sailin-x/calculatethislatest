import { calculatorRegistry } from '../../data/calculatorRegistry';
import { pet_care_calculatorCalculatorCalculator } from './pet_care_calculatorCalculatorCalculator';

export function registerpet_care_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new pet_care_calculatorCalculatorCalculator());
}

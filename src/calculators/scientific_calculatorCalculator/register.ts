import { calculatorRegistry } from '../../data/calculatorRegistry';
import { scientific_calculatorCalculatorCalculator } from './scientific_calculatorCalculatorCalculator';

export function registerscientific_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new scientific_calculatorCalculatorCalculator());
}

import { calculatorRegistry } from '../../data/calculatorRegistry';
import { photography_cost_calculatorCalculatorCalculator } from './photography_cost_calculatorCalculatorCalculator';

export function registerphotography_cost_calculatorCalculatorCalculator(): void {
  calculatorRegistry.register(new photography_cost_calculatorCalculatorCalculator());
}
